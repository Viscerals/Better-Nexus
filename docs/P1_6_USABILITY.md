# P1.6 assigned-Wishlist usability continuation

This experimental continuation starts at the actual reviewed P1.5.1 commit
`9e1ca04bcce16b85798687aa9e90f3b9ca5cdccb`, tree
`efff30c6e69b2757c666647b16fe5cc6a63f60d0`. Its complete directory, original
archives, regressions, failed probes and independent review remain unchanged.
The original build commit and imported review-snapshot commit remain distinct.

## Workflow and safety

The normal Orb view resolves the same assigned Wishlist as the main panel.
Start approves the displayed maximum and automatic use of eligible surplus
copies, including safe recycling after confirmed results. There is no second
target selector, source-count approval screen or per-result Start requirement.
Advanced retains explicit exclusions and the passive Recheck handler.

The runtime reserves rolled targets and unmet permanent targets, including the
editor's complete per-assignment permanent design. Permanent copies, mixed-quality
ID-only ambiguity, catalog-loss family barriers and explicit exclusions remain
protected. Eligibility and assignment binding are checked again inside the
actual adapter submission path after its fresh read.

Active-loadout, assignment-identity or relevant target changes pause further
mutations. A pending operation retains its original target and confirmation
evidence. A loadout change during a pending action makes its ownership response
uncorrelated; the receipt and exposure remain unresolved. Recheck, a return to
the old slot and reload cannot establish the missing correlation. An assignment
change on the same loadout permits passive settlement with the original evidence.
After confirmed settlement, explicit Resume adopts the new target and retains
the same maximum and spent/reserved exposure. Safe confirmed results continue
automatically until a defined terminal or pause condition.

Close hides only the detailed view. The main HUD has separate compact progress,
Pause/Resume and Stop controls. Ordinary Automation stays OFF when Orb mode ends.
Both permissions are revoked on world exit/entry. Reload cannot restart a run.

No completion proof was weakened. Equal-content same-ID/same-quality results
remain unresolved, possibly indefinitely, with pending ownership and exposure.
No automatic retry, refund or recovery-marker clearing is introduced.

## Demonstrated causes and limits

The coordinator reproduced blank Help in the actual P1.5.1 client. The opaque
parent rendered at level 129 while scroll, content and all four buttons rendered
at level 128, despite shown/visible=true. Body text was present, approximately
585 wide and 274 high. The original high requested frame levels caused the
observed child layering inversion. Help now uses levels 30/31/32, explicit
content anchoring and visible text measurement. The permanent-role picker uses
40/41/42 for its required assignment flow. All seven Help topics remain.

The coordinator's one baseline reload preserved the existing main assignment.
Native assignment loss was not reproduced. Synthetic startup did reproduce a
premature assignment prompt while the current loadout identity was unavailable.
The shared read-only assignment projection now reports restoration to the main
model/fontstring and Orb view. Durable exact contents and local permanent
designs survive reload. Name-only replacement and borrowing another loadout's
sole association were removed. Explicit assignments get a durable local ID;
reordered server slots and duplicate names do not authorize different contents.
An absent server mirror is explained while preserving the local exact plan;
absence alone is not treated as confirmed deletion.

The first candidate, `6f8de3d`, failed independent review despite 65 passing
prototype scripts. Two exact negative probes demonstrated a permanent-design
collision between plans sharing rolled contents and false result settlement
from another loadout. Both failed source/package receipts and the candidate
are preserved. Each saved assignment now carries detached, bounded permanent
rows with exact copies, quality and replacement intent. The normal main model,
editor, assignment picker and Orb projection consume that design. Legacy data
is preserved. Ambiguous server mirrors do not identify a row for overwrite;
the exact local plan remains readable. The loadout confirmation gate preserves
the unresolved operation across later notifications and reload.

## Validation boundary

The 54 prior prototype scripts are retained. Three UI scripts were updated for
the explicitly approved replacement of the old review/checkbox screen and the
move of Recheck to Advanced. Their source-protection, confirmation, exposure and
zero-mutation assertions remain. Fifteen new scripts exercise actual controller,
adapter, main/detail UI and restoration paths. The current inventory is 69.
The two independent negative probes are retained byte-for-byte as regressions.
The earlier positive loadout-switch fixture assumed correlation it did not
supply; it now requires retained pending exposure rather than false settlement.
Same-loadout target-change settlement and finite-budget Resume remain tested.

Tests run through the unchanged installed OBS LuaJIT 2.1 / Lua 5.1 ctypes route,
with the supplied LoadoutPilot reference. Ordinary policy and optimizer logic
are unchanged. Receipts and the fresh focused review belong to the delivery
handoff, not to a claim of native spending safety.

ORB AUTOMATION VERIFIED OFFLINE describes only the represented synthetic cases.
ORB SPENDING NOT TESTED — NO AUTHORIZATION.
Native Help, persistence and two-client Sync are coordinated separately against
the exact reviewed package. Their final results and temporary-record cleanup
must be read from the coordinator's native report. No native work occurs in the
writer checkout. No private backups or existing-player data enter this source.
